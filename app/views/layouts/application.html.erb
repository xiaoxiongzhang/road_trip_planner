<!DOCTYPE html>
<html>
<head>
  <title>Road Trip Planner</title>
  <meta http-equiv="content-type" content="text/html; charset=utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>
<header name="top">
  <nav class="navbar navbar-default nav-color" role="navigation" id="nav">
    <div class="container">
      <div class="navbar-header nav-color">
        <button id="nav_collapse" type="button" class="navbar-toggle toggle-btn" data-toggle="collapse" data-target="#bs-navbar-collapse-1" aria-controls="bs-navbar-collapse-1">
          <span class="fa fa-bars fa-lg"></span>
        </button>
        <a class="navbar-brand custom-logo-link" href="/">
          Road Trip Planner
        </a>
      </div>

      <div class="collapse navbar-collapse nav-submenu-color collapse" id="bs-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right custom">
          <li>
            <a href="/">Home</a>
          </li>

          <% if logged_in? %>
            <li><%= link_to "Trips", "/trip", method: 'get' %></li>
            <li><%= link_to "Museums", "/destination", method: 'get' %></li>
            <li><%= link_to "#{current_user.name}, logout", session_path(session[:user_id]), method: 'delete' %></li>
          <% else %>
            <li><%= link_to "login", new_session_path %></li>
          <% end -%>

        </ul>
      </div>
    </div>
  </nav>
</header>

<div class="container body-offset">
  <% if flash[:notice] %>
    <div class="alert alert-info"><%= flash[:notice] %></div>
  <% end -%>

  <%= yield %>
</div>

<%= javascript_include_tag 'manifest' %>
<%= yield :javascripts %>
</body>
</html>